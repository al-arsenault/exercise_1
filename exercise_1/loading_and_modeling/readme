These are the files required to import the data from the CMS website into our Hadoop instance, and to model it.
Start by running the load_data_lake.sh file, which will copy the files to hospitals.zip, then unzip the file
Then remove the header lines from the appropriate files, and finally move the relevant files into Hadoop.
